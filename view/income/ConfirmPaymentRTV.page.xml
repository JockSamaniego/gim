<?xml version="1.0" encoding="UTF-8"?>
<page login-required="true" no-conversation-view-id="/WorkdayList.xhtml"
	xmlns="http://jboss.org/schema/seam/pages" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jboss.org/schema/seam/pages http://jboss.org/schema/seam/pages-2.3.xsd"
	>
	
	<begin-conversation flush-mode="MANUAL" join="true" />
		
	<!-- navigation from-action="#{workdayHome.save()}">
		<rule if-outcome="persisted">
			<end-conversation before-redirect="true" />
			<redirect view-id="/income/WorkdayList.xhtml">
				<param name="isFromIncome" value="#{isFromIncome}" />
			</redirect>
		</rule>
		
		<rule if-outcome="updated">
			<end-conversation before-redirect="true" />
			<redirect view-id="/income/WorkdayList.xhtml">
				<param name="isFromIncome" value="#{isFromIncome}" />
			</redirect>
		</rule>
		<rule if-outcome="failed">
			<redirect view-id="/income/WorkdayEdit.xhtml" />
		</rule>
	</navigation>	
	
	<navigation from-action="#{workdayHome.remove}">
		<rule if-outcome="removed">
			<end-conversation before-redirect="true" />
			<redirect view-id="/income/WorkdayList.xhtml">
				<param name="isFromIncome" value="#{isFromIncome}" />
			</redirect>
		</rule>
	</navigation-->
</page>
