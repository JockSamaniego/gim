<?xml version="1.0" encoding="UTF-8"?>
<page xmlns="http://jboss.org/schema/seam/pages" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jboss.org/schema/seam/pages http://jboss.org/schema/seam/pages-2.3.xsd" login-required="true">

	<param name="firstResult" value="#{standList.firstResult}" />
	<param name="sort" value="#{standList.orderColumn}" />
	<param name="dir" value="#{standList.orderDirection}" />
	<param name="logic" value="#{standList.restrictionLogicOperator}" />

	<param name="from" />
	<param name="market" value="#{standList.market}"/>
	<param name="standStatus" value="#{standList.standStatus}"/>
	<param name="standType" value="#{standList.standType}"/>
	<param name="name" value="#{standList.criteria}" />

</page>
