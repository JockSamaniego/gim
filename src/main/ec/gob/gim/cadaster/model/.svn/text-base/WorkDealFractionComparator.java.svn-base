package ec.gob.gim.cadaster.model;

import java.util.Comparator;

public class WorkDealFractionComparator implements Comparator<WorkDealFraction> {

	@Override
	public int compare(WorkDealFraction o1, WorkDealFraction o2) {
		/*
		 * String str1 = o1.getAddress(); String str2 = o2.getAddress(); int
		 * comp = str1.compareTo(str2); int comp1 = str3.compareTo(str4);
		 * if(comp < 0) {
		 * 
		 * 
		 * return 1; } else { if (comp1 < 0) { return -1; } else if (comp1 > 0)
		 * { return 1; } else { return 0; } }
		 */
		String str3 = o1.getProperty().getCadastralCode();
		String str4 = o2.getProperty().getCadastralCode();
		return str3.compareTo(str4);

	}

}
